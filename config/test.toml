# Test config for running rMail services on non-privileged ports
[global]
mail_root = "mail_test"
tls_cert = "config/certs/localhost.crt"
tls_key = "config/certs/localhost.key"
log_level = "debug"
# optional web admin user for local testing
web_admin_user = "admin"
web_admin_password_hash = "plain:password"
# local test DB path for mailboxes/catchalls
db_path = "config/rmail_test.db"
# ACME challenge dir for local testing (empty by default)
acme_challenge_dir = "config/acme-challenges"

[global.tcp_listener]
reuse_port = false
backlog = 128
ipv6_only = true

[global.listeners]
smtp = ["127.0.0.1:2525", "[::1]:2525"]
smtps = ["127.0.0.1:2465", "[::1]:2465"]
submission = ["127.0.0.1:2587", "[::1]:2587"]
imap = ["127.0.0.1:1143", "[::1]:1143"]
imaps = ["127.0.0.1:1993", "[::1]:1993"]
admin = ["127.0.0.1:18080"]
webmail = ["127.0.0.1:18081"]

# Experimental: enable DANE/TLSA verification for outbound deliveries via environment variable
# RMAIL_ENABLE_DANE=1  (set to 0 or false to disable)
