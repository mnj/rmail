[package]
name = "rmail_common"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.7"
anyhow = "1.0"
argon2 = "0.4"
password-hash = "0.4"
rand = "0.8"
