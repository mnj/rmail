[package]
name = "rmail_queuectl"
version = "0.8.4"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
rmail_common = { path = "../common" }
serde_json = "1.0"
